<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.cy.tpes.mapper.hjx.WxFundTransactionMapper">
    <insert id="insertTrade" parameterType="java.util.Map">
        insert into fund_transaction (fttype, ftamount, ftdate, gcaccount)
        values (#{fttype},#{ftamount},#{ftdate},#{gcaccount})
    </insert>
</mapper>